# File: .github/workflows/publish.yml
name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master # Change this to your default branch

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Node.js ⚙️
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'

      - name: Install dependencies 🔧
        run: npm install

      - name: Build with Next.js 🏗️
        run: npm run build

      - name: Upload artifact 📡
        uses: actions/upload-artifact@v3
        with:
          name: github-pages # Ensure this name matches the one used in the deploy job
          path: out # Ensure this is the correct directory for your exported static site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Download artifact 📥
        uses: actions/download-artifact@v2
        with:
          name: github-pages # Ensure this name matches the one used in the build job

      - name: Publish to GitHub Pages 🚀
        uses: actions/deploy-pages@v4
